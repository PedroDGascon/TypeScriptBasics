(() => {

    //Revisar funciones void

    function callBatman() :void {
        return;
    }

    //arrow functions

    const callSuperman = (): void => {
        return;
    }

    const a = callBatman();
    console.log(a);

})()